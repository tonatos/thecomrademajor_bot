# TheComradeMajor Bot 🚔

Telegram бот с интеграцией GigaChat API, который играет роль сурового майора полиции и "анализирует" сообщения на предмет нарушений законодательства РФ в развлекательных целях.

## Функциональность

- ✅ Работает в групповых чатах Telegram
- ✅ Реагирует на упоминания (@thecomrademajor_bot) при ответе на сообщения
- ✅ Использует GigaChat API для генерации ответов в стиле сурового полицейского
- ✅ Создает смешные "обвинения" даже для безобидных сообщений

## Установка и настройка

### 1. Установка зависимостей

Проект использует Poetry для управления зависимостями:

```bash
# Установите Poetry если еще не установлен
curl -sSL https://install.python-poetry.org | python3 -

# Установите зависимости проекта
task install
# или
poetry install
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните следующие переменные:

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# GigaChat API Configuration  
GIGACHAT_CLIENT_ID=your_gigachat_client_id
GIGACHAT_CLIENT_SECRET=your_gigachat_client_secret
GIGACHAT_SCOPE=GIGACHAT_API_PERS

# Bot Configuration
BOT_USERNAME=thecomrademajor_bot
LOG_LEVEL=INFO
```

### 3. Получение токенов

#### Telegram Bot Token:
1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Создайте нового бота командой `/newbot`
3. Следуйте инструкциям и получите токен
4. Настройте бота для работы в группах: `/setprivacy` → Disable

#### GigaChat API:
1. Перейдите на [developers.sber.ru](https://developers.sber.ru/docs/ru/gigachat/api/main)
2. Зарегистрируйтесь и создайте приложение
3. Получите Client ID и Client Secret

## Использование

### Запуск бота

```bash
# Запуск в режиме разработки
task dev

# Обычный запуск
task run

# Запуск с проверками
task check
```

### Запуск через Docker

Для быстрого развертывания используйте Docker Compose:

```bash
# Создайте папку для логов
mkdir -p logs

# Запуск в фоновом режиме
docker compose up -d --build

# Просмотр логов
docker compose logs -f bot

# Остановка
docker compose down
```

### Использование в Telegram

1. Добавьте бота в групповой чат
2. Ответьте на любое сообщение
3. В своем ответе упомяните бота: `@thecomrademajor_bot`
4. Бот проанализирует исходное сообщение и выдаст "обвинение"

Пример:
```
Пользователь А: "Сегодня хорошая погода!"
Пользователь Б (отвечая): "@thecomrademajor_bot проверь это сообщение"
Бот: "🚔 ВНИМАНИЕ! Незаконное распространение метеорологической информации без лицензии! Статья 228.1 КоАП РФ - штраф до 50,000 рублей!"
```

## Команды бота

- `/start` - Приветствие и описание функций
- `/help` - Справка по использованию

## Разработка

### Доступные команды (Taskfile)

```bash
task install      # Установка зависимостей
task run         # Запуск бота
task dev         # Запуск в режиме разработки
task check       # Проверка кода (линтинг, типы, форматирование)
task lint        # Линтинг кода
task type-check  # Проверка типов
task format      # Форматирование кода
task format-check # Проверка форматирования
task test        # Запуск тестов
```

### Структура проекта

```
src/
├── __init__.py
├── main.py           # Точка входа
├── bot.py            # Основная логика бота
├── gigachat_client.py # Клиент для GigaChat API
└── config.py         # Конфигурация
```

## Технологии

- **Python 3.11+**
- **python-telegram-bot** - Telegram Bot API
- **httpx** - HTTP клиент для GigaChat API
- **pydantic** - Валидация данных и настройки
- **Poetry** - Управление зависимостями
- **Taskfile** - Автоматизация задач
- **Docker** - Контейнеризация приложения

## Предупреждение

⚠️ **Бот создан исключительно в развлекательных целях!** Все "обвинения" и "статьи" являются выдуманными и не имеют отношения к реальному законодательству РФ.

## Лицензия

MIT License
